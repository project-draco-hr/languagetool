{
  final String[] parts=str.split(" ");
  for (int i=0; i < parts.length; i++) {
    if (isHyphenIgnored() || !"-".equals(parts[i])) {
      if (StringTools.isAllUppercase(parts[i])) {
        return true;
      }
    }
  }
  return false;
}
