{
  try {
    validateInternal(this.getClass().getResourceAsStream(filename),this.getClass().getResource(xmlSchema));
  }
 catch (  Exception e) {
    throw new IOException("Cannot load or parse '" + filename + "'",e);
  }
}
