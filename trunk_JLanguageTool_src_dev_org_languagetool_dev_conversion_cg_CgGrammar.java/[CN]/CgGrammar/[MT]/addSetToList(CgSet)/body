{
  if (s.number == 0) {
    if (this.sets_list.isEmpty() || this.sets_list.get(0) != s) {
      if (!s.sets.isEmpty()) {
        for (        int setID : s.sets) {
          this.addSetToList(this.getSet(setID));
        }
      }
      s.number=this.sets_list.size();
      this.sets_list.add(s);
    }
  }
}
