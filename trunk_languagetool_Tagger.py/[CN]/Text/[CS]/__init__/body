def __init__(self):
    self.count_unambiguous = 0
    self.count_ambiguous = 0
    self.count_unknown = 0
    self.nonword = re.compile('([\\s,.!?]+)')
    self.bnc_word_regexp = re.compile('<W\\s+TYPE="(.*?)".*?>(.*?)</W>', (re.DOTALL | re.IGNORECASE))
    return
