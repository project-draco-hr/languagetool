{
  String defaultPatternFilename=RULES_DIR + "/" + language.getShortName()+ "/"+ PATTERN_FILE;
  List<PatternRule> patternRules=loadPatternRules(defaultPatternFilename);
  userRules.addAll(patternRules);
}
