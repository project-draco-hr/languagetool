{
  if (listUnknownWords) {
    AnalyzedTokenReadings[] atr=analyzedText.getTokensWithoutWhitespace();
    for (int i=0; i < atr.length; i++) {
      AnalyzedTokenReadings t=atr[i];
      if (t.getReadings().toString().equals("[null]"))       unknownWords.add(t.getToken());
    }
  }
}
