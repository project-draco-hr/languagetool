{
  if (this.dictionary == null || this.manualTagger == null || this.compoundTokenizer == null) {
synchronized (this) {
      if (this.dictionary == null || this.manualTagger == null || this.compoundTokenizer == null) {
        this.initialize();
      }
    }
  }
}
