{
  if (dictionary == null || manualTagger == null || compoundTokenizer == null) {
synchronized (this) {
      if (dictionary == null || manualTagger == null || compoundTokenizer == null) {
        initialize();
      }
    }
  }
}
