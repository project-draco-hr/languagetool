{
  int pos=startPos;
  String token="";
  do {
    if (pos >= tokens.size()) {
      return -1;
    }
    token=(String)tokens.get(pos);
    pos++;
  }
 while (token.trim().equals(""));
  return pos - 1;
}
