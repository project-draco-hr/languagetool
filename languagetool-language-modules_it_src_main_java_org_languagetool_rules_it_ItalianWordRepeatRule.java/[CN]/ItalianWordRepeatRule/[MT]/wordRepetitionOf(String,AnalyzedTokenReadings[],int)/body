{
  if (position > 2)   return (tokens[position - 1].getToken().equals(word) && tokens[position].getToken().equals(word));
  if (position == 2)   return (tokens[1].getToken().equalsIgnoreCase(word) && tokens[2].getToken().equals(word));
  return false;
}
