{
  StringBuffer sb=new StringBuffer();
  for (int i=0; i < tokens.length; i++) {
    if (tokens[i] != null && tokens[i].isWhitespace())     sb.append(tokens[i].getToken());
 else     sb.append(tokens[i]);
  }
  return sb.toString();
}
