{
  List<String> list=new ArrayList<>();
  list.add("foo");
  list.add("bar");
  list.add(",");
  assertEquals("foo,bar,,",String.join(",",list));
  assertEquals("foo\tbar\t,",String.join("\t",list));
}
