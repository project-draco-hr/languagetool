{
  AtomFeedItem item=getSummary("summary-table.txt");
  assertThat(item.getOldContent().size(),is(0));
  assertThat(item.getNewContent().size(),is(1));
  assertThat(item.getNewContent().get(0),is("Besetzung"));
}
