{
  UkrainianSynthesizer synth=new UkrainianSynthesizer();
  assertEquals(synth.synthesize(dummyToken("??????????????????????"),"??????????????????????").length,0);
  assertEquals("[??????????]",Arrays.toString(synth.synthesize(dummyToken("??????????"),"noun:n:v_rod")));
  assertEquals("[????????????????]",Arrays.toString(getSortedArray(synth.synthesize(dummyToken("????????????????"),"adj:f:v_rod*",true))));
  assertEquals("[????????????????, ????????????????, ??????????????????, ????????????????]",Arrays.toString(getSortedArray(synth.synthesize(dummyToken("????????????????"),"adj:.:v_oru",true))));
}
