{
  if (sEvent.equals("execute")) {
    try {
      String text=getText();
      checkText(text);
    }
 catch (    Throwable e) {
      e.printStackTrace();
    }
  }
 else {
    System.err.println("Sorry, don't know what to do, sEvent = " + sEvent);
  }
}
