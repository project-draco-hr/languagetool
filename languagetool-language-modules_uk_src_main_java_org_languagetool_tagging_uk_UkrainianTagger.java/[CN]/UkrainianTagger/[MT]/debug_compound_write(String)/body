{
  if (compoundDebugWriter == null)   return;
  try {
    compoundDebugWriter.append(word);
    compoundDebugWriter.newLine();
    compoundDebugWriter.flush();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
