{
  if (baseElement.isReferenceElement()) {
    final int refPos=firstMatchToken + baseElement.getMatch().getTokenRef();
    if (refPos < tokens.length) {
      element=baseElement.compile(tokens[refPos],language.getSynthesizer());
    }
  }
}
