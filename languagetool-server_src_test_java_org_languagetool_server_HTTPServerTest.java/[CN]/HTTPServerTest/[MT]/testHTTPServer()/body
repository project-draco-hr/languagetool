{
  HTTPServer server=new HTTPServer();
  assertFalse(server.isRunning());
  try {
    server.run();
    assertTrue(server.isRunning());
    runTestsV1();
    runTestsV2();
  }
  finally {
    server.stop();
    assertFalse(server.isRunning());
  }
}
