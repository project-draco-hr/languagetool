{
  boolean isSE=false;
  for (  AnalyzedToken reading : anTokReadings)   if (reading.posTag != null) {
    isSE=isSE || reading.posTag.equals(JLanguageTool.SENTENCE_END_TAGNAME);
  }
  return isSE;
}
