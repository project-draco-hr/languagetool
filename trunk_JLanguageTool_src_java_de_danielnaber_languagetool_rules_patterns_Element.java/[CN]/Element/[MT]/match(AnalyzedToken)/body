{
  boolean matched=false;
  if (testString) {
    matched=(matchStringToken(token) != negation) && (matchPosToken(token) != posNegation);
  }
 else {
    matched=(!negation) && (matchPosToken(token) != posNegation);
  }
  if (andGroupSet) {
    andGroupCheck[0]|=matched;
  }
  return matched;
}
