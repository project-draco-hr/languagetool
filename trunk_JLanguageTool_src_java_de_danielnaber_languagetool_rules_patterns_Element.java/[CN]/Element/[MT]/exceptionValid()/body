{
  boolean eNext=false;
  if (exceptionSet) {
    Iterator<Element> it=exceptionList.iterator();
    while (it.hasNext()) {
      eNext=eNext || it.next().exceptionValidNext;
      if (eNext) {
        break;
      }
    }
  }
  return eNext;
}
