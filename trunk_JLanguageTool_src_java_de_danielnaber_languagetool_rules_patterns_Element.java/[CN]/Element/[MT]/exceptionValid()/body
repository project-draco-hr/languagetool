{
  boolean eNext=false;
  if (exceptionSet) {
    for (    Element testException : exceptionList) {
      eNext|=testException.exceptionValidNext;
      if (eNext) {
        break;
      }
    }
  }
  return eNext;
}
