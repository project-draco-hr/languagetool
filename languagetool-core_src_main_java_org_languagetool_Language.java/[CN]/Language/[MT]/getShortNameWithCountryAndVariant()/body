{
  String name=getShortName();
  if (getCountries().length == 1 && !name.contains("-x-")) {
    name+="-" + getCountries()[0];
    if (!getVariant().isEmpty()) {
      name+="-" + getVariant();
    }
  }
  return name;
}
