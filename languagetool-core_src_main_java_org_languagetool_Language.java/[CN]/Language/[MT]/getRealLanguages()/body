{
  List<Language> result=new ArrayList<>();
  for (  Language lang : LANGUAGES) {
    if (!"xx".equals(lang.getShortName())) {
      result.add(lang);
    }
  }
  return result.toArray(new Language[result.size()]);
}
