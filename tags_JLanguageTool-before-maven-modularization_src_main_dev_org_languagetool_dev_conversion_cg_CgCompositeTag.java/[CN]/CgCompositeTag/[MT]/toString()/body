{
  StringBuilder sb=new StringBuilder();
  sb.append("(");
  for (int i=0; i < tags.size(); i++) {
    if (i == tags.size() - 1) {
      sb.append(tags.get(i).tag + ")");
    }
 else {
      sb.append(tags.get(i).tag + " ");
    }
  }
  return sb.toString();
}
