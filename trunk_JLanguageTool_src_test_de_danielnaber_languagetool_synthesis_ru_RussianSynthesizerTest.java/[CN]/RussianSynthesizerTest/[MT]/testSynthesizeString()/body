{
  RussianSynthesizer synth=new RussianSynthesizer();
  assertEquals(synth.synthesize(dummyToken("blablabla"),"blablabla").length,0);
  assertEquals("[??????????]",Arrays.toString(synth.synthesize(dummyToken("??????????"),"NN:Fem:Sin:Nom")));
  assertEquals("[??????????]",Arrays.toString(synth.synthesize(dummyToken("??????????"),"NN:Fem:Sin:R")));
}
