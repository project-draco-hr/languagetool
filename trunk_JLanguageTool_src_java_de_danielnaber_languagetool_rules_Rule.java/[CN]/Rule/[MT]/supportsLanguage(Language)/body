{
  Language[] languages=Language.LANGUAGES;
  for (int i=0; i < languages.length; i++) {
    Set<String> relevantIDs=language.getRelevantRuleIDs();
    if (relevantIDs != null && relevantIDs.contains(getId()))     return true;
  }
  return false;
}
