{
  if (firstTime) {
    firstTime=false;
    for (    Spans subSpan : subSpans) {
      if (!subSpan.next()) {
        more=false;
        return false;
      }
    }
    more=true;
  }
  if (collectPayloads) {
    matchPayload.clear();
  }
  return advanceAfterOrdered();
}
