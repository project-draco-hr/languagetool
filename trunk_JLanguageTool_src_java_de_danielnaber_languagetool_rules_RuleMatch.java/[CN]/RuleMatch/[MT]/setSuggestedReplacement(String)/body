{
  if (repl == null)   throw new NullPointerException("replacement might be empty but not null");
  final List<String> fixes=new ArrayList<String>();
  fixes.add(repl);
  setSuggestedReplacements(fixes);
}
