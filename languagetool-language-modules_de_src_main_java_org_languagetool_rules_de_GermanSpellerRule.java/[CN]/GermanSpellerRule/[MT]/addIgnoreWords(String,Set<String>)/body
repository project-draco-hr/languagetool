{
  String line;
  if (language.getShortNameWithCountryAndVariant().equals("de-CH")) {
    line=origLine.replace("??","ss");
  }
 else {
    line=origLine;
  }
  wordsToBeIgnored.addAll(expandLine(line));
}
