{
  try {
    for (int i=0; i < Language.LANGUAGES.length; i++) {
      Language lang=Language.LANGUAGES[i];
      validate("rules" + File.separator + lang.getShortName()+ File.separator+ "grammar.xml");
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail();
  }
}
