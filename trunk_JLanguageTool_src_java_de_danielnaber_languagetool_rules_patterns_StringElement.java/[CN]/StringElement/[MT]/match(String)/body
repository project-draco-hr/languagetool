{
  if (caseSensitive) {
    for (int i=0; i < tokens.length; i++) {
      if (tokens[i].equals(token))       return true;
    }
  }
 else {
    for (int i=0; i < tokens.length; i++) {
      if (tokens[i].equalsIgnoreCase(token))       return true;
    }
  }
  return false;
}
