{
  if (caseSensitive) {
    for (int i=0; i < tokens.length; i++) {
      if (tokens[i].equals(token.getToken()))       return true;
    }
  }
 else {
    for (int i=0; i < tokens.length; i++) {
      if (tokens[i].equalsIgnoreCase(token.getToken()))       return true;
    }
  }
  return false;
}
