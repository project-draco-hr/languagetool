{
  GenericUnpairedBracketsRule rule=new GenericUnpairedBracketsRule(TestTools.getEnglishMessages(),Language.SPANISH);
  RuleMatch[] matches;
  JLanguageTool langTool=new JLanguageTool(Language.SPANISH);
  matches=rule.match(langTool.getAnalyzedSentence("Soy un hombre (muy honrado)."));
  assertEquals(0,matches.length);
  matches=rule.match(langTool.getAnalyzedSentence("De d??nde vas?"));
  assertEquals(1,matches.length);
  matches=rule.match(langTool.getAnalyzedSentence("??Atenci??n"));
  assertEquals(1,matches.length);
}
