{
  Locale[] aLocales=new Locale[Language.LANGUAGES.length];
  for (int i=0; i < Language.LANGUAGES.length; i++) {
    aLocales[i]=new Locale(Language.LANGUAGES[i].getShortName(),Language.LANGUAGES[i].getLocale().getVariant(),"");
  }
  return aLocales;
}
