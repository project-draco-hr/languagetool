{
  if (tokSequence != null) {
    AnalyzedTokenReadings atr=new AnalyzedTokenReadings(tokSequence.toArray(new AnalyzedToken[tokSequence.size()]));
    tokSequence.clear();
    return atr;
  }
 else {
    return null;
  }
}
