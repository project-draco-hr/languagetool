{
  if (event.getSource() == okButton || event.getActionCommand() == null) {
    replaceText=textField.getText();
  }
 else   if (event.getSource() == cancelButton) {
  }
 else {
    System.err.println("Unknown action: " + event);
  }
  dialog.setVisible(false);
}
